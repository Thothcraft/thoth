name: Build & Release

on:
  push:
    tags:
      - 'v*'          # Trigger on version tags like v1.0.0

permissions:
  contents: write      # Allow creating releases

jobs:
  # ── macOS app bundle ─────────────────────────────────────────────────────
  build-mac:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install py2app
        run: |
          cd thoth_mac
          pip install py2app

      - name: Build .app bundle
        run: |
          cd thoth_mac
          python setup_app.py py2app

      - name: Package Thoth.app
        run: |
          cd thoth_mac
          zip -r Thoth-macOS.zip build/Thoth.app

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: macos-release
          path: thoth_mac/Thoth-macOS.zip

  # ── Windows archive ────────────────────────────────────────────────────
  build-win:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Create Windows archive
        run: |
          zip -r Thoth-Windows.zip \
            thoth_core/ thoth_win/ .env LICENSE README.md

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-release
          path: Thoth-Windows.zip

  # ── Raspberry Pi archive ───────────────────────────────────────────────
  build-rpi:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Create RPi archive
        run: |
          tar czf Thoth-RPi.tar.gz \
            thoth_core/ thoth_rpi/ .env LICENSE README.md

      - name: Upload RPi artifact
        uses: actions/upload-artifact@v4
        with:
          name: rpi-release
          path: Thoth-RPi.tar.gz

  # ── Create GitHub Release ──────────────────────────────────────────────
  release:
    needs: [build-mac, build-win, build-rpi]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-assets

      - name: List assets
        run: find release-assets -type f

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            release-assets/macos-release/Thoth-macOS.zip
            release-assets/windows-release/Thoth-Windows.zip
            release-assets/rpi-release/Thoth-RPi.tar.gz
          body: |
            ## Thoth ${{ github.ref_name }}

            ### Downloads

            | Platform | File | Install |
            |----------|------|---------|
            | **macOS** | `Thoth-macOS.zip` | Extract → Double-click Thoth.app to install to /Applications |
            | **Windows** | `Thoth-Windows.zip` | Extract → `cd thoth_win && powershell -ExecutionPolicy Bypass -File install.ps1` |
            | **Raspberry Pi** | `Thoth-RPi.tar.gz` | Extract → `sudo ./thoth_rpi/setup/install.sh` |

            ### What's New
            See commit history for details.
